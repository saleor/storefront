fragment SelectedAttributeDetailsFragment on SelectedAttribute {
  attribute {
    id
    name
    translation(languageCode: $locale) {
      id
      name
    }
    type
    unit
  }
  values {
    id
    name
    translation(languageCode: $locale) {
      id
      name
      richText
    }
    value
  }
}
