overwrite: true
schema: "https://vercel.saleor.cloud/graphql/"
documents: "components/**/*.tsx"
generates:
  saleor/api.tsx:
    plugins:
      - add:
          content: "// THIS FILE IS GENERATED WITH `npm run generate`"
      - "typescript"
      - "typescript-operations"
      - "typescript-react-apollo"
    config:
      dedupeOperationSuffix: true # Prevent suffix duplication in generated names
  ./graphql.schema.json:
    plugins:
      - "introspection"
