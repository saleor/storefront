# Verify: plp-shell-split

```bash
pnpm exec tsc --noEmit
```

- [ ] Category/collection hero renders without waiting for grid filters
- [ ] Filters/pagination still work in grid Suspense island
- [ ] Cached getters use `cacheLife("catalog")` or manifest helper

Record: `2026-06-plp-shell-split` / `3be1d7e6`
