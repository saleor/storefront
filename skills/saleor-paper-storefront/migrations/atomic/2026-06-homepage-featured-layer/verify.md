# Verify: homepage-featured-layer

```bash
pnpm exec tsc --noEmit
```

- [ ] Featured products load on homepage
- [ ] `"use cache"` only in `get-featured-products.ts`, not inline in page
- [ ] Product cards use server navigation (channel-prefixed href) where upstream does

Record: `2026-06-homepage-featured-layer` / `b0e1b869`
