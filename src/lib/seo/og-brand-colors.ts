/**
 * Hex colors for `/api/og` ImageResponse (no CSS variables in Satori).
 * Keep in sync with default `:root` tokens in `src/styles/brand.css`.
 */
export const ogBrandColors = {
	background: "#FAF9F7",
	foreground: "#1A1A1A",
	mutedForeground: "#737373",
	card: "#FFFFFF",
	border: "#E5E4DF",
} as const;
