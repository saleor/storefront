fragment OrderDetails on Order {
	id
	number
	created
	total {
		gross {
			amount
			currency
		}
	}
	lines {
		variant {
			id
			name
			translation(languageCode: $languageCode) {
				name
			}
			product {
				id
				name
				description
				slug
				translation(languageCode: $languageCode) {
					name
				}
				thumbnail {
					url
					alt
				}
				category {
					id
					name
				}
			}
			pricing {
				price {
					gross {
						amount
						currency
					}
				}
			}
		}
		quantity
	}
	status
	statusDisplay
	paymentStatus
}
