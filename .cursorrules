# Saleor Storefront - AI Agent Rules

You are working on a Saleor e-commerce storefront built with Next.js 16, TypeScript, and Tailwind CSS.

## Skills

Two skill sources are available:

**Project skill** (`skills/saleor-paper-storefront/`) -- Saleor storefront patterns:

| Task | Rule |
|------|------|
| Architecture / canonical Next.js (start here) | `paper-architecture` |
| Modifying `.graphql` files | `data-graphql` |
| Caching, ISR, webhooks | `data-caching` |
| Storefront marketing copy layer | `data-storefront-content` |
| Saleor Models for storefront copy | `data-storefront-content-saleor` |
| Storefront content attribute types | `data-storefront-content-attributes` |
| Product detail page (PDP) | `product-pdp` |
| Variant selection on PDP | `product-variants` |
| Product filtering/sorting | `product-filtering` |
| Storefront vs checkout surfaces | `paper-surfaces` |
| BFF auth / session / account PPR | `data-auth-routes` |
| Checkout UX / design principles | `checkout-design-principles` |
| Checkout flow debugging | `checkout-management` |
| Adding a payment gateway | `checkout-payment-gateways` |
| Checkout UI components | `checkout-components` |
| Design tokens & variant matrix | `ui-design-system` |
| Design quality / world-class bar | `design-quality-rubric` |
| Marketing section catalog | `ui-sections` |
| Molding PDP / homepage layout | `page-composition` |
| Designing from a prompt or image | `design-from-image` |
| Verifying design (gates, a11y) | `design-verification` |
| File naming, exports, imports | `references/code-conventions` |
| Creating/styling components | `ui-components` |
| Channels, fulfillment & stock | `ui-channels` |
| Locale + channel URL routing | `ui-locale-routing` |
| Code-owned UI strings (next-intl) | `ui-i18n` |
| SEO, metadata, OG images | `seo-metadata` |
| Third-party widget embeds (reviews, scripts) | `third-party-embeds` |
| Investigating Saleor API | `dev-investigation` |
| Mobile dev via ngrok / LAN | `dev-local` |
| Upgrading a forked shop | `migrations/SKILL.md` |

**External skills** — pinned in `skills-lock.json`; `pnpm skills:bootstrap` runs `npx skills experimental_install`. Cursor scans `.agents/skills/` and `.cursor/skills/`, not repo-root `skills/`.

| Task | Skill |
|------|-------|
| Saleor GraphQL API patterns | `saleor-storefront` |
| Writing React components | `vercel-react-best-practices` |
| Component composition | `vercel-composition-patterns` |
| UI accessibility/UX review | `web-design-guidelines` |

## Critical Commands

```bash
pnpm run generate           # After src/graphql/*.graphql changes
pnpm run generate:checkout  # After src/checkout/graphql/*.graphql changes
pnpm exec tsc --noEmit      # Type check
pnpm run build              # Full build
```

## Key Rules

1. **GraphQL**: Run `pnpm generate` (storefront) or `pnpm generate:checkout` (checkout) after `.graphql` changes
2. **Components**: Use Server Components by default, `"use client"` only when needed
3. **Styling**: Use design tokens (`bg-background`, `text-foreground`), not hardcoded colors
4. **Nullable fields**: Handle nulls intentionally -- optional chaining for display values, guards/throws when null is a real problem
5. **Imports**: Use `@/` alias (`import { Button } from "@/ui/components/ui/button"`)

## File Locations

| Purpose | Location |
|---------|----------|
| Storefront GraphQL | `src/graphql/*.graphql` |
| Checkout GraphQL | `src/checkout/graphql/*.graphql` |
| Generated types (DO NOT EDIT) | `src/gql/`, `src/checkout/graphql/generated/` |
| UI components | `src/ui/components/` |
| Design tokens | `src/styles/brand.css` |
| SEO helpers | `src/lib/seo/` |
| Filter utilities | `src/ui/components/plp/filter-utils.ts` |

## Documentation

- **Full docs**: `AGENTS.md` in project root
- **Project skill**: `skills/saleor-paper-storefront/SKILL.md`
- **Fork migrations**: `skills/saleor-paper-storefront/migrations/SKILL.md` + `paper-version.json`
- **Code conventions**: `skills/saleor-paper-storefront/references/code-conventions.md`
- **External skills**: `skills-lock.json` + `pnpm skills:bootstrap` (symlink + external installs)
