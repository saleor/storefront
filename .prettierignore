pnpm-lock.yaml
.next
# Machine-written SoT — formatting is owned by formatStorefrontContentSnapshotJson
# (JSON.stringify + tabs). Prettier collapses short arrays and breaks content:verify-seed.
config/saleor/storefront-content.snapshot.json
